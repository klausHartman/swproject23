Bio_Embeddings.ipynb:
1) Embedding computation of an input VOG (fasta-file) using Bio-Embeddings libary from Dallago et al.
2) Dimensionality reduction (tSNE, 3 dimensions)
3) 3D visualization via plotly-express
4) Training and validation of a one-class SVM

SeqVec.ipynb:
1) Embedding computation via SeqVec (according to the SeqVec repository)

Prototype.ipynb:
- Containing various trials and notes about error encounters
- Containing SeqVec command line approach (worked once but not consistently)